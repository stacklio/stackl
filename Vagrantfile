Vagrant.configure("2") do |config|
  config.vm.box = "stacklio/centos7-devbox"
  config.vm.box_version = "1.0.0"
  config.vm.provider "virtualbox" do |vb|
    vb.memory = "4000"
    vb.cpus = 3
  end
end
