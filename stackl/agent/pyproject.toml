[tool.poetry]
name = "agent"
version = "0.1.2"
description = ""
authors = ["Frederic <frederic.vanreet@icloud.com>"]

[tool.poetry.dependencies]
python = "^3.7"
grpcio = "^1.29.0"
protobuf = "^3.12.2"
kubernetes = "^11.0.0"
pyyaml = "^5.3.1"
stackl_client = "^0.1.1"
stackl-protos = {path = "../../library/stackl_protos"}

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
start-agent = "agent.main:start"


[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
