all:
  hosts:
    localhost:
      ansible_connection: local

nodes:
  children:
    swarm_manager_main:
      hosts:
        tm001.stackl.io:
          env: $ENV_VAR
          ansible_user: ansible
          use_lfs: yes
          agent_broker_type:
          docker_swarm_labels:
            - redis=master
            - purpose_api=true