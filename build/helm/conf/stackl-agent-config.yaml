terraform:
  create_command: ["terraform"]
  create_args:
    - "apply"
    - "--auto-approve"
    - "-var-file=/opt/terraform/plan/variables.json"
  delete_command: TODO
  variables:
    format: json
    filename: variables.json
  image_pull_secrets:
    - dome-nexus
