kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "{{ template "stackl.rest" . }}"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi