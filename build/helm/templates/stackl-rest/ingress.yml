apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "{{ template "stackl.rest" . }}"
  labels:
    {{- include "stackl.labels" . | nindent 4 }}
    component: rest
spec:
  rules:
    - host: {{ .Values.stacklRest.hostname }}
      http:
        paths:
          - path: /
            backend:
              serviceName: "{{ template "stackl.rest" . }}"
              servicePort: 8080