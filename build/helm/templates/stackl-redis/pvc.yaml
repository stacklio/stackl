kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "{{ template "stackl.redis" . }}"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi