apiVersion: apps/v1
kind: Deployment
metadata:
  name:  "{{ template "stackl.agent" . }}"
  labels:
    {{ include "stackl.labels" . | nindent 4 }}
    component: agent
spec:
  replicas: {{ .Values.stacklAgent.replicas }}
  selector:
    matchLabels:
      {{- include "stackl.selectorLabels" . | nindent 6 }}
      component: agent
  template:
    metadata:
      labels:
        {{- include "stackl.selectorLabels" . | nindent 8 }}
        component: agent
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ template "stackl.agent" . }}-stackl-agent
      containers:
        - name: stackl-agent
          image: "{{ .Values.stacklAgent.image }}"
          resources:
            requests:
              memory: 64Mi
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          volumeMounts:
            - name: stackl-agent-config
              mountPath: /etc/stackl-agent
          env:
            - name: "STACKL_HOST"
              value: "{{ template "stackl.rest" . }}:8080"
            - name: "STACKL_GRPC_HOST"
              value: "{{ template "stackl.rest" . }}:50051"
            - name: STACKL_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: SERVICE_ACCOUNT
              valueFrom:
                fieldRef:
                  fieldPath: spec.serviceAccountName
            - name: "PYTHONUNBUFFERED"
              value: "TRUE"
            - name: "SECRET_HANDLER"
              value: "{{ .Values.stacklAgent.secretHandler }}"
            - name: "AGENT_ID"
              value: "{{ .Values.stacklAgent.agentName }}"
            - name: "AGENT_HOST"
              value: "{{ template "stackl.agent" . }}"
            - name: "AGENT_PORT"
              value: "50051"
            - name: "AGENT_SELECTOR"
              value: "{{ .Values.stacklAgent.selector }}"
            {{- with .Values.stacklAgent.vaultAddr }}
            - name: "VAULT_ADDR"
              value: "{{ . }}"
            {{- end }}
            {{- with .Values.stacklAgent.vaultRole }}
            - name: "VAULT_ROLE"
              value: "{{ . }}"
            {{- end }}
            {{- with .Values.stacklAgent.vaultMountPoint }}
            - name: "VAULT_MOUNT_POINT"
              value: "{{ . }}"
            {{- end }}
      volumes:
        - name: stackl-agent-config
          configMap:
            name: stackl-agent-config
